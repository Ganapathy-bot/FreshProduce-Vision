# Streamlit Community Cloud — NO TensorFlow (Python 3.14 has no TF wheels).
# Inference uses ONNX Runtime + NumPy only.
streamlit>=1.28.0
onnxruntime>=1.16.0
numpy>=1.23.0
Pillow>=9.0.0
pandas>=1.5.0
